curl -i -X POST -H "Accept:application/json" -H "Content-Type:application/json" http://localhost:8084/connectors/ -d '{
    "name": "mongo-connector",
    "config": {
        "connector.class": "io.debezium.connector.mongodb.MongoDbConnector",
        "tasks.max": "1",
        "database.hostname": "mongo",
        "database.port": "27018",
        "database.user": "root",
        "database.password": "root",
        "database.server.name": "mongo-server",
        "mongodb.name": "mongodatabase",
        "collection.whitelist": ".*",
        "database.history.kafka.bootstrap.servers": "kafka:29092",
        "database.history.kafka.topic": "schema-changes.mongodb",
        "database.history.store.only.monitored.tables.ddl": "false",
        "database.history.skip.unparseable.ddl": "true"
    }
}'

curl -i -X POST -H "Accept:application/json" -H "Content-Type:application/json" http://localhost:8084/connectors/ -d '{
    "name": "mongo-connector",
    "config": {
        "connector.class": "io.debezium.connector.mongodb.MongoDbConnector",
        "tasks.max": "1",
        "database.hostname": "mongo",  # Aquí debes especificar la dirección del host de MongoDB
        "database.port": "27018",
        "database.user": "root",
        "database.password": "adrian1234",
        "database.server.name": "mongo-server",
        "mongodb.name": "your_mongo_database_name",
        "collection.whitelist": ".*",
        "database.history.kafka.bootstrap.servers": "kafka:29092",
        "database.history.kafka.topic": "schema-changes.mongodb",
        "database.history.store.only.monitored.tables.ddl": "false",
        "database.history.skip.unparseable.ddl": "true"
    }
}'
