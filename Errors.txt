1) The task was FAILED!
Caused by: io.debezium.DebeziumException: Failed to start replication stream at LSN{0/23791C8}; when setting up multiple connectors for the same database host, please make sure to use a distinct replication slot name for each.
Caused by: org.postgresql.util.PSQLException: ERROR: replication slot "debezium" is active for PID 37

# Problema con los slots de replicación en PostgreSQL. Un slot de replicación es un objeto de PostgreSQL que permite que un cliente de replicación (como Debezium) lea cambios de la base de datos.

-> SELECT * FROM pg_replication_slots WHERE slot_name = 'debezium';
-> SELECT pg_terminate_backend(<PID>);
# Esto garantiza que cada conector tenga su propio slot y no interfiera con otros conectores.


2) Error while validating connector config: Postgres server wal_level property must be 'logical' but is: 'replica'
# El error que estás viendo indica que Debezium espera que la propiedad wal_level del servidor PostgreSQL esté configurada en 'logical'
Se necesita modificar postgresql.conf -> wal_level = logical

-> SHOW wal_level;
-> ALTER SYSTEM SET wal_level = 'logical';
-> SELECT pg_reload_conf();


3) Caused by: org.postgresql.util.PSQLException: ERROR: could not access file "decoderbufs": No such file or directory
# Este módulo es necesario para la replicación lógica y es utilizado por Debezium.
-> SHOW shared_preload_libraries;
decoderbufs debe estar incluido
postgresql.conf -> shared_preload_libraries = 'decoderbufs'
